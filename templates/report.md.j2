# Integration Analysis Report

**Generated by mainger-agent**
**Date:** {{ date | default("(filled by agent)") }}

## 1. Data summary

- Internal sample size: **{{ n_int }}**
- External sample size: **{{ n_ext | default("(not provided)") }}**
- Number of predictors: **{{ p }}**
- Detected sharing regime: **{{ regime }}**

## 2. Theoretical guarantees

- Beneficial range upper bound: $\eta^\star = ${{ eta_star }}
- Selected $\eta$: **{{ eta_used }}** {% if tuning_method %}(via {{ tuning_method }}){% endif %}
{% if concordance_verdict %}
- Concordance verdict (full regime only): **{{ concordance_verdict }}**
- Spectral advantage at chosen $\eta$: $\mathcal{A}(\eta) = ${{ advantage }}
{% endif %}

## 3. Integrated estimator

| Variable | Internal | External | Integrated |
|----------|----------|----------|------------|
{% for row in coefficients_table -%}
| {{ row.variable }} | {{ row.internal }} | {{ row.external }} | {{ row.integrated }} |
{% endfor %}

## 4. Performance

- Estimated MSE (internal-only): {{ mse_internal }}
- Estimated MSE (integrated): {{ mse_integrated }}
- Relative improvement: {{ relative_improvement }}

## 5. Recommendation

{{ recommendation }}

## 6. Caveats

{{ caveats }}

---
*This report was produced via tool calls to the `mainger` R package (v0.2.0).
All numerical values originate from package output, not LLM estimation.
The full agent trace is in `trace.json` for reproducibility.*
